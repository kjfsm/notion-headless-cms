# AGENTS

このリポジトリのエージェント向け指示は `CLAUDE.md`（絶対ルール・索引）と
`.claude/project.md`（プロジェクト固有ガイド）を参照してください。

このファイルは他エージェント互換のためのスタブです。

---

## コードコメント方針

**残すべきコメント（2種類のみ）**

1. **WHY** — コードだけでは意図がわからない非自明な制約・バグ回避策・驚くべき不変条件
2. **export API の使い方説明** — JSDoc `@param` / `@returns` / `@example` など利用者向けドキュメント

**書いてはいけないコメント**

- 変更経緯・diff 的な説明（「〜を修正した」「〜に変更した」など）
- コードを読めばわかる WHAT の説明（型名・関数名で自明なもの）
- セクション区切りバナー（`// ──────` / `// --------` など）
- 次の行に何をするかの逐次説明

コメントは日本語で書く（`CLAUDE.md` 言語ルールに準拠）。
