# AGENTS

エージェント向け指示の本体は `CLAUDE.md`（絶対ルール・索引）と `.claude/project.md`（プロジェクト固有ガイド）を参照してください。
このファイルは他エージェントとの互換のために設けています。

---

## 作業姿勢

- **より良いアプローチがあれば、作業を始める前に提案・確認する。** 実装してから「実はこうした方がよかった」と伝えない。
- **指示が目的とずれている可能性を常に考える。** 表面の指示をそのまま実行するのではなく、ユーザーが本当に達成したいことの本質を考え、おかしいと思ったら確認する。

---

## コードコメント方針

**残すもの（2種類のみ）**

1. **WHY** — コードだけでは意図がわからない非自明な制約・バグ回避策・驚くべき不変条件
2. **export API の使い方** — JSDoc `@param` / `@returns` / `@example` など利用者向けドキュメント

**書かないもの**

- 変更経緯・diff 的な説明（「〜を修正した」「〜に変更した」など）
- コードを読めばわかる WHAT の説明（型名・関数名で自明なもの）
- セクション区切りバナー（`// ──────` / `// --------` など）
- 次の行に何をするかの逐次説明

コメントは日本語で書く（`CLAUDE.md` 言語ルールに準拠）。
