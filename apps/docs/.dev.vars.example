# このファイルを .dev.vars にコピーして値を設定（git 管理対象外）
# 本番環境では `wrangler secret put <NAME>` で登録する

# Notion API トークン (必須)
NOTION_TOKEN=secret_xxxx

# Notion Webhook 受信時の署名検証用シークレット (任意)
# POST /api/cms/webhook を使う場合のみ設定
NOTION_WEBHOOK_SECRET=
