> **このドキュメントは v1→v2 移行時の歴史的記録です。**
> `@notion-headless-cms/client` を含む v2 系パッケージは削除され、
> 現行の唯一のアーキテクチャは `@notion-headless-cms/cms` です。

# 1.0 リリース凍結チェックリスト (Issue #319 / M8)（歴史的記録）

`@notion-headless-cms/*` を 1.0.0 に揃えるための判断材料と作業手順をまとめる。
短期 S1〜S7・中期 M1〜M7 は完了済み（→「ロードマップ達成状況」）。
v2.0（#346）で単一エントリ `createCMS` 化・メタパッケージ廃止まで完了しており、
1.0 では **fixed mode への収束** を主な残タスクとして扱う
（公開名の無印集約（RFC §7）は npm 名前規則により取り下げ、`@notion-headless-cms/client` を恒久名とする）。

---

## 凍結対象 (各項目 ✅ で完了)

### 公開 API シェイプ

- [ ] `createCMS` の引数 / 戻り値 (Issue #312 / M1 → v2.0 #346 で単一エントリ集約・実装済み / 凍結は rc 時)
  - 旧 `createCms` / メタパッケージ（node/cloudflare/next）は v2.0 で削除済み
  - 公開名は `@notion-headless-cms/client`（+ サブパス）を恒久名とする（RFC §7 の無印集約は npm 規則により取り下げ）
- [ ] 各 preset の戻り値: `{ cache, swr, hooks?, rateLimiter?, waitUntil? }` (Issue #313 / M2)
- [ ] `defineCollection<T>()` / `StrictCollectionDef<T>` の型契約 (Issue #314 / M3)
- [ ] `CMSAdapter` / `DataSourceAdapter` / `CacheAdapter` のメソッドシグネチャ
- [ ] `CMSError` コード体系 (`BuiltInCMSErrorCode` literal union, Issue #307 / S3)
- [ ] CLI 生成物 (`nhc.schema.ts`) フォーマット

### 後方互換性

- [x] `@deprecated` 付与 API をすべて削除 (S6 / M1 / M4 のリスト)
  - `notion-orm` の `blocks` / `ogp` / `enrichers` → 削除済み（changeset: notion-orm minor）
  - 各ランタイムの `createCms` / `CreateCmsOptions` → v2.0 で削除済み
  - リポジトリ内の `@deprecated` マークは 0 件
- [ ] `docs/ja/migration/` の各ガイドが最新仕様と一致

### 品質ゲート

- [ ] `pnpm verify:ci` (build / typecheck / test / publint / attw) 緑
- [ ] `pnpm size` がしきい値内
- [ ] `pnpm lint:boundary` 緑 (internal/ 越境なし)
- [ ] `e2e-nightly.yml` が直近 7 日間グリーン
- [ ] `publish-dry-run-nightly.yml` が直近 7 日間グリーン

### ドキュメント

- [ ] `docs/ja/quickstart.md` が 1.0 API で動く
- [x] 全 examples が `createCMS` で動く（v2.0 で移行済み）
- [ ] `docs/ja/migration/` の v1.0.0 削除予定 API が CHANGELOG に列挙されている

---

## ロードマップ達成状況（棚卸し・2026-06）

v2.0 までに完了した範囲と、1.0 に残る範囲を一望する。

| 区分           | 項目                                                                                                       | 状況                                         |
| -------------- | ---------------------------------------------------------------------------------------------------------- | -------------------------------------------- |
| 短期 S1–S7     | JSDoc / changeset 運用 / エラー辞書 / minimal-node / テスト底上げ / deprecated 整理 / docs サイト          | ✅ 完了（#305–#311, tracking #332）          |
| 中期 M1–M7     | createCMS 集約 / preset→runtime / 型推論 / internal 越境禁止 / docs 拡充 / E2E・契約テスト / リリース gate | ✅ 完了（#312–#318, tracking #333）          |
| react-renderer | Context 化 / URL 変換フック / Image・Link slot / srcSet                                                    | ✅ 完了（#217–#219, tracking #222）          |
| 中期 M8        | 1.0 判断材料（本ファイル）                                                                                 | 🟡 進行中（本チェックリスト）                |
| 長期           | i18n / 汎用 DataSource / OpenTelemetry / プレビュー / Streaming SSR・RSC ほか                              | ⬜ #334 に集約                               |
| 機能           | katex/shiki/OGP の fetch 戦略両対応                                                                        | ⬜ #300                                      |
| 機能           | Notion 内部リンクの自動 slug 変換                                                                          | ⬜ #260（`resolvePageUrl` フックは提供済み） |

> 旧・大トラッキング #328（S/M/L 各 8 項目）は #332 / #333 / #334 へ再編済み。

---

## バージョン整列方針: fixed mode（採用確定）

`.changeset/config.json` を以下に切替:

```json
{
  "$schema": "https://unpkg.com/@changesets/config/schema.json",
  "changelog": "@changesets/cli/changelog",
  "commit": false,
  "fixed": [["@notion-headless-cms/*"]],
  "linked": [],
  "access": "public",
  "baseBranch": "main",
  "updateInternalDependencies": "patch",
  "ignore": []
}
```

- fixed mode により全 `@notion-headless-cms/*` パッケージが常に同じ番号で公開される
- 利用者は「どれを入れれば良いか」を平易に判断できる (1.0.0 同期)

### 代替案

「core / notion-source / notion-orm のみ 1.0、他は独立 SemVer」も検討したが、
バージョン体系の混在 (現状 core 0.3.x / meta 1.0.x / fetch-\* 0.0.x) を再生産する
ため非推奨。1.0 リリース時に fixed mode へ収束させる。

---

## 1.0-rc.0 リリース手順

1. 本ファイルのチェックリストをすべて ✅
2. fixed mode への切替 PR を作る:
   ```bash
   # .changeset/config.json を fixed mode に編集
   pnpm changeset add  # catch-up changeset (major) を作成
   ```
3. main へマージ → changesets/action が Version Packages PR を作成
4. Version Packages PR で `1.0.0-rc.0` がすべて揃っていることを確認
5. dry-run:
   ```bash
   pnpm install --frozen-lockfile
   pnpm build
   pnpm -r --filter="./packages/*" publish --dry-run --tag rc
   ```
6. `release-dry-run.yml` が PR 上で緑になっていることを確認
7. Version Packages PR をマージ → release.yml が `--tag rc` で npm publish

## 1.0.0 GA への昇格

`1.0.0-rc.N` で 2 週間以上の運用試験を経たのち、

```bash
pnpm changeset add  # bump 種別: minor (rc.N → 1.0.0)
```

で `1.0.0` Version Packages PR を作る。`--tag latest` で publish される。

---

## 関連リンク

- ロードマップ tracking: #332（短期・完了）/ #333（中期・完了）/ #334（長期）
- migration ガイド: [`docs/ja/migration/`](../migration/)
- meta package 連鎖 bump: [`docs/ja/release/meta-package-bump.md`](./meta-package-bump.md)
- リリースワークフロー: `.github/workflows/release.yml` / `release-dry-run.yml` / `publish-dry-run-nightly.yml`
