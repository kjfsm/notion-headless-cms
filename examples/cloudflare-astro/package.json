{
	"name": "example-cloudflare-astro",
	"private": true,
	"version": "0.0.4",
	"type": "module",
	"scripts": {
		"astro": "astro",
		"build": "nhc generate && astro build",
		"cf-typegen": "wrangler types",
		"check": "nhc generate && astro build && tsc && wrangler deploy --dry-run",
		"deploy": "wrangler deploy",
		"dev": "nhc generate && astro dev",
		"preview": "nhc generate && astro build && wrangler dev"
	},
	"dependencies": {
		"@notion-headless-cms/adapter-cloudflare": "workspace:*",
		"@notion-headless-cms/core": "workspace:*",
		"@notion-headless-cms/notion-orm": "workspace:*",
		"astro": "^6.1.9",
		"zod": "^4.3.6"
	},
	"devDependencies": {
		"@astrojs/check": "^0.9.8",
		"@astrojs/cloudflare": "^13.2.0",
		"@notion-headless-cms/cli": "workspace:*",
		"@types/node": "^25.6.0",
		"typescript": "^6.0.3",
		"wrangler": "^4.84.1"
	}
}
