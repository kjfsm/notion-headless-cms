{
	"name": "example-cloudflare-astro",
	"private": true,
	"version": "0.0.16",
	"type": "module",
	"scripts": {
		"astro": "astro",
		"build": "astro build",
		"cf-typegen": "wrangler types",
		"check": "astro build && tsc && wrangler deploy --dry-run",
		"deploy": "wrangler deploy",
		"dev": "astro dev",
		"generate": "nhc generate",
		"preview": "astro build && wrangler dev"
	},
	"dependencies": {
		"@notion-headless-cms/cache-r2": "workspace:*",
		"@notion-headless-cms/core": "workspace:*",
		"@notion-headless-cms/notion-orm": "workspace:*",
		"@notion-headless-cms/renderer": "workspace:*",
		"astro": "^6.1.9",
		"zod": "^4.3.6"
	},
	"devDependencies": {
		"@astrojs/check": "^0.9.8",
		"@astrojs/cloudflare": "^13.2.0",
		"@notion-headless-cms/cli": "workspace:*",
		"@types/node": "^25.6.0",
		"dotenv": "^17.4.2",
		"typescript": "^6.0.3",
		"wrangler": "^4.84.1"
	}
}
