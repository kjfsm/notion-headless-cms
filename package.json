{
  "name": "@kjfsm/notion-headless-cms",
  "version": "0.0.1",
  "description": "Notion headless CMS toolkit (monorepo)",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "format": "pnpm -r format",
    "type-check": "pnpm -r type-check"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.12",
    "@cloudflare/workers-types": "^4.20260410.1",
    "tsup": "^8",
    "typescript": "^5"
  }
}
